{
  "framework": null,
  "outputDirectory": "public",
  "installCommand": "npm ci",
  "buildCommand": "node src/bootstrap.js && node src/once.js && npm run refresh:groups",
  "rewrites": [{ "source": "/", "destination": "/index.dc.html" }]
}
